// This file previously contained an alternate implementation of the
// keyboard shortcuts modal. Keep a stable re-export so other imports
// that reference `KeyboardShortcutsNew` continue to work while the
// canonical implementation remains in `KeyboardShortcuts.tsx`.

export { default } from "./KeyboardShortcuts";
