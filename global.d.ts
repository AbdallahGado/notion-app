/* global.d.ts - make jest-dom matchers available to TypeScript */
import "@testing-library/jest-dom";

declare global {
  // allow importing .css in tests and code (if not already handled)
}

export {};
